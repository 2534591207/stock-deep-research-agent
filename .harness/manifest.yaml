# .harness/manifest.yaml — generated by init-harness v2
# This file records the harness installation state. Re-run `init --bootstrap-manifest` to update.

schema_version: "2.0"
topology: custom
generated_at: "2026-06-07T13:29:08Z"
generated_by: "init-harness v2"
project_name: "股票 Deep Research Agent"

# Harness file checksums (SHA-256, first 12 chars). Used by check-index.sh.
checksums:
  workflow.md: "computed-at-install"
  rules/_index.md: "computed-at-install"
  sensors/check-all.sh: "computed-at-install"

# Rule counts at last baseline
rule_counts:
  core: 5
  L1: 0
  L2: 0
  L3: 0

# Model assumptions recorded at install time
model_assumptions:
  context_window: "unknown"
  model_id: "unknown"

# Install options used
install_options:
  hooks_installed: "false"
  ci_installed: "false"
