# .harness/project.yaml — generated by init-harness v2
# DO NOT edit the harness-managed fields (between HARNESS:START and HARNESS:END).
# Fields outside those markers are yours to customize.

# HARNESS:START
topology: custom
harness_version: "2.0"
generated_at: "2026-06-07T13:29:08Z"
project_name: "股票 Deep Research Agent"
# HARNESS:END

# --- Customize below ---

# Human-readable project name (shown in AGENTS.md header)
project_name_display: "股票 Deep Research Agent"

# Stack summary (shown in AGENTS.md)
# Examples: "Python FastAPI + PostgreSQL", "Go + gRPC + Redis", "Node/TS + Express + MongoDB"
stack_summary: "Python + FastAPI + vanilla HTML/CSS/JS"

# Primary language(s). Used by sensors to locate source files.
# Examples: python, go, typescript, rust, ruby
primary_language: "python"

# Source root(s) relative to repo root. Comma-separated if multiple.
# Examples: "src", "src,pkg", "services/api,services/worker"
source_roots: "src,web"

# Test directories relative to repo root.
test_roots: "tests"

# Team preferences (optional)
# design_review_required: true      # default: true for L2+
# acceptance_review_required: true  # default: true for L2+
# waiver_ttl_days: 30               # default: 30
