# 两层对话 Agent · 后端依赖（运行环境 = Python 3.11 · venv）
# 用框架不造轮子：编排/记忆/LLM/HTTP/数值/图/排版 全用成熟库；只手写金融口径 + 薄胶水

# Web 服务
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9    # multipart 表单/文件上传（POST /upload）

# 编排 + LLM（核心）
langgraph>=0.2.0            # create_react_agent + MemorySaver（不手写 agent 循环/session）
langchain-openai>=0.1.0     # ChatOpenAI
langchain-core>=0.2.0       # @tool

# 配置 / 模型（pydantic-settings 自带 .env 读取，无需 python-dotenv）
pydantic>=2.6
pydantic-settings>=2.2

# 数值计算（不手撸 for 循环）
pandas>=2.0
numpy>=1.26

# 报告
matplotlib>=3.8             # 归一化走势 PNG
jinja2>=3.1                 # 报告排版

# 行情数据（免费、无需 key、覆盖美股与 ADR 含 BABA）
yfinance>=0.2.40
httpx>=0.27

# 识别（resolver 英文名模糊匹配）
rapidfuzz>=3.6

# 测试
pytest>=8.0
pytest-asyncio>=0.23
pymupdf>=1.27              # 财报 PDF 文本提取（POST /upload 文档问答）
