# 股票 Deep Research Agent — CLAUDE Project Memory

<!-- HARNESS:START v2 topology=custom -->
> This block is managed by `init-harness`. Do not edit between markers.

**Project**: 股票 Deep Research Agent (agent)
**Topology**: custom
**Stack**: unknown / unknown

## Working in this repo — required reading (in order)

1. `./AGENTS.md` — the map
2. `.harness/README.md` — harness control system overview
3. `.harness/playbooks/L<tier>-*.md` — pick by task classification

## Cardinal rules

- L2+ work goes through PRD → design → review-packet → reviewer gate.
- L3 requires DCO `Signed-off-by:` from a named human.
- Run `bash .harness/sensors/check-all.sh` before any commit.
- Reviewer is always a fresh agent instance.

## Common commands

- Build: `echo 'no build command configured'`
- Test: `echo 'no test command configured'`
- Run: `echo 'no run command configured'`
- Lint: `echo 'no lint command configured'`
- Sensor suite: `bash .harness/sensors/check-all.sh`

<!-- HARNESS:END -->
